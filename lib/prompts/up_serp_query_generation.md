Given the following prompt from the user, _generate a list of SERP queries to research the topic._ Return a maximum of {num_queries} queries, but feel free to return less if the original prompt is clear. Make sure each query is unique and not similar to each other.

{query_addon}
